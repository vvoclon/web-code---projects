<html>
    <link rel="stylesheet" type= "text/css" href="static/styles/styles_projekt1.css">
    <body>
    <style>
        body {
            background-color: #867070;
        }
        h1 {
        text-align: center;
        color: #E4D0D0;
        position: relative;
        margin-left: -2%;
        margin-right: -2%;}
    </style>
    <h1>
        Descriptive statistics
    </h1>
    <table class="Desc stats">
        
        <tr>
            {% for header in headings2 %}
            <th>
                {{header}}
            </th>
            {% endfor %}
        </tr>
            {% for row in desc_data %}
            <tr>
                {% for cell in row %}
                <td>{{cell}}</td>
                {% endfor %}
                </tr>
            {% endfor %}
        </tr>
    </table>
    <style>
    table, th, td {border:1px solid #000000;
    border-style: solid;
    text-align: center;
    color: #F5EBEB;
    margin-left: auto;
    margin-right: auto;
    }
</style>
    <h1>
        Shapiro test results - for normal distribution
    </h1>
    <table class="Shapiro">
        <tr>
            {% for header in headings %}
            <th>
                {{header}}
            </th>
            {% endfor %}
        </tr>
            {% for row in data %}
            <tr>
                {% for cell in row %}
                <td>{{cell}}</td>
                {% endfor %}
                </tr>
            {% endfor %}
        </tr>
    </table>
    <h1>Test results</h1>
     <table class="Table">
        <tr>
            {% for header in headings1 %}
            <th>
                {{header}}
            </th>
            {% endfor %}
        </tr>
            {% for row in data_1 %}
            <tr>
                {% for cell in row %}
                <td>{{cell}}</td>
                {% endfor %}
                </tr>
            {% endfor %}
        </tr>
    </table>
    <a href="https://github.com/vvoclon/web-code---projects" style="text-align:center; font-size:30px;" ><h3>Github link</h3></a>
    </body>
</html>
